{
    "layer": "top",
    "position": "top",
    "margin-top": 6,
    "margin-right": 8,
    "margin-left": 8,
    "height": 40,
    "spacing": 10,
    "reload_style_on_change": true,
    
    "modules-left": ["custom/power", "hyprland/workspaces"],
    "modules-center": ["custom/clock"],
    "modules-right": ["tray", "custom/spotify", "custom/system-temp", "bluetooth", "network", "pulseaudio", "custom/menu"],
    
    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate",
        "all-outputs": true,
        "persistent-workspaces": {
            "*": [1, 2]
        }
    },
  
    "custom/system-temp": {
        "exec": "cpu=$(sensors | grep 'Tctl:' | awk '{print int($2)}' | tr -d '+°C'); gpu=$(nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader,nounits); echo \"CPU ${cpu}°C  GPU ${gpu}°C\"",
        "format": "󰍛 ",
        "tooltip-format": "{}",
        "interval": 5,
        "cursor": true,
        "on-click": "gnome-system-monitor"
    },

    "custom/cpu-temp": {
        "exec": "sensors | grep 'Tctl:' | awk '{print int($2)}' | tr -d '+°C'",
        "format": "󰍛 {}°C",
        "tooltip-format": "CPU {}°C",
        "interval": 5
    },

    "custom/gpu-temp": {
        "exec": "nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader,nounits",  // For NVIDIA
        // "exec": "cat /sys/class/drm/card0/device/hwmon/hwmon*/temp1_input | awk '{print $1/1000}'",  // For AMD
        "format": "󰾲 {}°C",
        "tooltip-format": "GPU {}°C",
        "interval": 5,
        "return-type": ""
    },

    "tray": {
      "icon-size": 20,
      "spacing": 10,
    },
    
    "bluetooth": {
        "format": " 󰂯 ",
        "format-disabled": " 󰂲 ",
        "format-connected": " 󰂱 ",
        "on-click": "blueman-manager",
    },

    "network": {
        "format-wifi": "󰤨 ",
        "format-ethernet": "󰈀 ",
        "format-disconnected": "󰤮 ",
        "tooltip-format-wifi": "{essid} ({signalStrength}%)\n{ipaddr}/{cidr}",
        "tooltip-format-ethernet": "Connected {ipaddr}/{cidr} ↓{bandwidthDownBytes} ↑{bandwidthUpBytes}",
        "tooltip-format-disconnected": "Disconnected",
        "on-click": "nm-connection-editor"
    },
    
    "pulseaudio": {
        "format": "{icon} ",
        "format-muted": "{volume}%",
        "format-icons": {
            "default": ["", "", ""]
        },
        "tooltip-format": "{volume}%",
        "on-click": "pavucontrol"
    },

    "custom/clock": {
      "exec": "~/.config/waybar/scripts/clock.sh",
      "interval": 30,
      "on-click": "gnome-calendar"
    },
    
    "custom/power": {
        "format": "",
        "tooltip": false,
        "on-click": "wlogout"
    },

    "custom/spotify": {
        "format": "♫ {}",
        "tooltip": true,
        "max-length": 20,
        "interval": 1,
        "return-type": "json",
        "exec": "vol=$(playerctl -p spotify volume 2>/dev/null | awk '{printf \"%.0f%%\", $1*100}'); info=$(playerctl -p spotify metadata --format '{{ artist }} - {{ title }}' 2>/dev/null | sed 's/\"/\\\\\"/g'); echo \"{\\\"text\\\":\\\"$vol\\\",\\\"tooltip\\\":\\\"$vol $info\\\"}\" || echo '{\"text\":\"\",\"tooltip\":\"\"}'",
        "exec-if": "playerctl -p spotify status 2>/dev/null",
        "on-click": "playerctl -p spotify next",
        "on-click-right": "playerctl -p spotify previous",
        "on-click-middle": "playerctl -p spotify play-pause",
        "on-scroll-up": "playerctl -p spotify volume 0.05+",
        "on-scroll-down": "playerctl -p spotify volume 0.05-"
    },
    
    "custom/menu": {
      "format": "󰂚 ",
      "on-click": "swaync-client -t -sw",
      "tooltip-format": "Control Panel"
    }
},
